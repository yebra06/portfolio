import React, { Component } from 'react';
import {
    BrowserRouter,
    Link,
    Route
} from 'react-router-dom';

const Router = () => (
    <BrowserRouter>
        <div>

        </div>
    </BrowserRouter>
);

export default Router;
